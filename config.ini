[ssh]
host=...
port=22
username=root
# ssh password is read from the AD_SSH_PASSWD env variable

[paths]
remote_dir=~/
remote_tar=~/backup.tar.gz
local_tar=backup.tar.gz

[discord]
# REQUIRED: id of the discord server (guild) where you want to create the category/channels
guild_id=...
# OPTIONAL: reuse an existing category by name or numeric ID, leave empty to create a new one
category=